"""Root URLconf.

Vazio por design neste bloco de scaffold. Healthcheck (`/health/`, `/ready/`),
landing e rotas das apps entram em blocos posteriores da Sprint 1.
"""

from django.urls import path

urlpatterns: list[path] = []
